import React from 'react'

import { Button } from 'react-bootstrap';

const AddPost = () => {

	const handleSubmit = () => {

	}

  return (
	<div>
	  <h4>Add Post</h4>

	  <Button 
	  	type="butimport { Button } from 'bootstrap';
		  ton"
		onClick={handleSubmit}
	  >
		  Add Post
	  </Button>
	</div>
  )
}

export default AddPost
